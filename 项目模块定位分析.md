# 项目模块定位与差异辨析

> 本文档理清项目中名称相近、功能可能存在交叉的模块之间的精确边界。
> 文档随项目演进而维护，当新增模块或调整功能归属时及时更新。

---

## 一、规划项目库 vs 互动资源管理

| | 规划项目库 (achievement) | 互动资源管理 (resource) |
|------|------|------|
| 管理对象 | 规划**项目**（Project） | 源网荷储设备**模型**（Model） |
| 阶段 | 规划 → 可研 → 审批 → 建设 → 投运 | 脱离项目独立存在，可在规划或运行期使用 |
| 颗粒度 | 项目级（如"阳光电站50MW"） | 设备模型级（物理特性/控制策略/接口参数） |
| 核心动作 | 可行性评估、并网核验、成效验证 | 模型构建、电厂/设备注册、拓扑关系 |
| 本质 | 项目全生命周期管理台账 | 仿真参数模型库 + 设备资产目录 |
| 关系 | 项目投运后，其设备可注册为 resource 中的电厂 | resource 模型可被规划方案引用，也可被场景模拟调用 |

**没有重复**：一个管项目是否可行，一个管设备模型长什么样。

---

## 二、设备台账动态管理 vs 资源维护

两者管理的是同一套底层数据（`equipment` 表），但**业务视角不同**：

| | 设备台账动态管理 (planning/EquipmentLedger) | 资源维护 (resource/ResourceMaintenance) |
|------|------|------|
| 归属 | 挂在一个规划方案的 pv_station 下 | 全局设备视角，不依赖规划方案 |
| 核心视角 | **时间线**：设备一生经历了什么 | **当前状态**：设备现在健康吗、还能用多久 |
| 数据重心 | 生命周期事件流（一条设备 N 条事件） | 可靠性评分、寿命预测、模型绑定 |
| 本质 | 设备履历追溯 | 设备健康监控 |
| 生命周期覆盖 | 设计选型 → 采购 → 投运 → 运行 → 维护 → 退役 | 关注运行阶段的当前快照 |

> **注意**：规划阶段的设备实体 `pv_stations` 与运维阶段的设备实体 `power_plants` 是不同表中的不同概念——前者是规划配置（"预计安装"），后者是建成注册（"已安装运行"）。

**没有重复**：同一张 equipment 表，规划台账看事件履历，资源维护看当前指标。

---

## 三、互动资源管理 vs 互动场景库

| | 互动资源管理 (resource) | 互动场景库 (scenario) |
|------|------|------|
| 职责 | 构建和管理源网荷储**参数模型** | 基于模型构建**虚拟运行场景**，模拟多主体协同 |
| 产出 | 可复用的技术模型库 | 场景实例 + 策略 + 模拟结果 |
| 联动 | 提供 `power_plants`/`equipment`/`resource_models` 数据 | 场景搭建时选择 resource 中的电厂、设备、模型作为接入点 |

**没有重复**：resource 是模型底座，scenario 是基于底座的仿真应用。

---

## 四、模块间数据流

```
规划项目库 (achievement)
  └─ 项目投运 ──→ 互动资源管理 (resource) 注册电厂 + 设备 + 模型
                       ├─ 设备维护：健康评估、寿命预测
                       └─ 提供模型 ──→ 互动场景库 (scenario) 搭建虚拟场景
                                            ├─ 互动场景管理（场景 CRUD）
                                            ├─ 互动场景策略管理（协同策略）
                                            ├─ 场景模拟与验证（启动模拟/实时监控）
                                            ├─ 场景执行效果评估（评估报告）
                                            └─ 场景策略人工干预（暂停/调整/追溯）
```

---

## 五、各模块管理的实体层次

| 实体 | 所属模块 | 说明 |
|------|---------|------|
| **项目** (projects) | achievement | 规划项目，如"阳光电站50MW并网项目" |
| **方案** (plans) | planning | 配电网规划方案 |
| **规划电站** (pv_stations) | planning | 规划方案下的光伏电站配置 |
| **电厂** (power_plants) | resource | 源网荷储体系下的实际电厂注册 |
| **设备** (equipment) | planning + resource 共用 | planning 看履历，resource 看健康 |
| **资源模型** (resource_models) | resource | PV_ABSORPTION / PV_OUTPUT / CAPACITY / STORAGE / LOAD |
| **互动场景** (interactive_scenarios) | scenario | 虚拟运行场景 |
| **策略** (scenario_strategies) | scenario | 源网荷储协同策略 |
| **模拟** (scenario_simulations) | scenario | 场景模拟执行记录 |
| **评估** (scenario_evaluations) | scenario | 模拟效果评估报告 |
| **干预** (scenario_interventions) | scenario | 人工干预操作日志 |
